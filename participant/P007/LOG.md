# 实验日志

## 2026-09-23：E001 官方模板基线

- 使用复制到 P007 目录的官方 PPO 模板及原始 `policy.npz`。
- 本地公开评测 `status=ok`，`provenance=local_preview`。
- `performance_score=66.6667`；basic `mean_j=0`，cooperation `mean_j=0.1333333`。
- 两组平均碰撞率均为 0。

## 2026-09-23：E002 确定性规则策略 v1

- 唯一主要变量：将 `entry.py` 的模板 PPO 推理替换为确定性规则控制；官方套件、种子和评测程序不变。
- 对可见目标执行局部距离竞价；较近机器人获得目标，等距时以较小机器人编号打破平局。
- 没有可见目标时前往按机器人编号分配的内圈搜索航点；追踪使用带速度阻尼的控制器，并叠加近距离队友斥力。
- 提交预检通过；8 个公开回合全部 `status=ok`，结果为本地预览。
- `performance_score=183.3333`；basic `mean_j=0.0833333`，cooperation `mean_j=0.2833333`；两组平均碰撞率均为 0。
- 相对 E001 提升 116.6666 分（2.75 倍）。
- 失败场景明显：`coop-0` 的两次重复均为 0；`coop-1` 均为 0.5666667，说明搜索航点与局部分配对初始布局敏感。

## 2026-09-23：E003 目标历史与速度估计

- 在 E002 基础上只增加目标历史：记录最后绝对位置和可见步，利用相邻观测估算并平滑目标速度。
- 目标离开视野后最多外推 4 步，并按目标场地边界进行反射修正；记忆过期后恢复原搜索行为。
- 提交预检通过；8 个公开回合全部 `status=ok`，`performance_score=183.3333`，碰撞率仍为 0。
- 各场景结果与 E002 完全相同，因此本次改动在公开套件上没有可测得的分数增益。
- 可能原因：产生得分的轨迹没有经历影响决策的短暂丢失，零分场景则可能从一开始就没有发现目标，历史机制无法凭空建立轨迹。

## 2026-09-23：E004 删除竞价失败 fallback

- 在 E003 基础上只删除一个行为：机器人输掉目标竞价后，不再继续追逐最近的可见目标，而是进入原有搜索流程。
- 提交预检通过；8 个公开回合全部 `status=ok`，`performance_score=183.3333`，全部逐回合指标与 E003 相同。
- `coop-0` 中分配已从 `[0, 0, None]` 变为 `[None, 0, None]`，说明修改确实生效。
- 分数不变的原因是机器人 1 追逐目标 0 的轨迹没有变化，最后最近距离仍为 0.179，未达到 0.15 的覆盖半径；机器人 0 和 2 的搜索也未在十步内发现目标 1、2。
- 结论：取消重复追逐使分工语义更清楚，但单独使用不能解决 `coop-0`，下一瓶颈是搜索路径或目标 0 的接近速度。

## 2026-09-23：E005 提高追踪方向增益

- 以 E004 为基线，只将追踪方向增益从 5.0 提高至 7.0。
- 提交预检和 8 回合公开测试通过，`performance_score=183.3333`，所有逐回合指标与 E004 相同。
- `coop-0` 最后一步的最近距离仍为 0.179。
- 原因：相关追踪距离下 `min(1.0, gain * distance)` 已经达到动作上限 1.0，提高增益没有实际改变动作。

## 2026-09-23：E006 移除远距离追踪阻尼

- 恢复 E004 的追踪方向增益 5.0，只将目标距离不小于 0.18 时的速度阻尼从 0.35 降为 0.0；近距离阻尼仍为 1.2。
- 提交预检和 8 回合公开测试通过，`performance_score=183.3333`，碰撞率仍为 0。
- `coop-0` 最后一步最近距离由 0.179 改善至 0.163，但仍未进入 0.15 的目标覆盖半径，因此分数没有变化。
- 公开套件其他逐回合指标没有下降。当前保留该版本作为下一实验的候选基线。

## 2026-09-23：E007 追踪动作按轴分别饱和

- 以 E006 为基线，只改变追踪驱动的映射方式：从单位方向上限制总模长，改为 x、y 两个动作分量分别裁剪到 `[-1, 1]`。
- 该改动利用了协议允许的二维对角驱动能力；分配、历史、搜索、阻尼和避碰参数均未改变。
- 提交预检与 8 回合公开测试通过，`performance_score=216.6667`，碰撞率仍为 0。
- basic `mean_j` 从 0.0833333 提升到 0.1；cooperation `mean_j` 从 0.2833333 提升到 0.3333333。
- `coop-0` 在第 7 步首次进入覆盖范围，最后三步最近距离依次为 0.149、0.121、0.107，回合 `mean_j=0.1`。
- 相比 E006 总分提升 33.3334，且公开套件中没有场景指标下降，因此保留为当前方案。
